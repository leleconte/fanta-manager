# L-STORE FANTA MANAGER — GitHub Pages

Carica **il contenuto** di questa cartella nella root del repository GitHub.

La root deve contenere `index.html`, `iniziale.html`, `css/`, `js/`, `data/` e `assets/`.

In GitHub: Settings → Pages → Deploy from a branch → main → /(root).
